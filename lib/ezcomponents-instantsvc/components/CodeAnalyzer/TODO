[done] add a php.ini if possible to prevent any code execution in the sandbox process
   -   implement summarizeInterfaces
   -   investigate problems with reading from pipes.
       leads to deadlogs on crawling through source tree
   -   implement a new class which can be ask to rate a specific metric
       on the base of some configurable values in a ini file for example